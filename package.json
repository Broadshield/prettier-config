{
  "name": "@Broadshield/prettier-config",
  "description": "Prettier configuration for Broadshield",
  "main": "prettierrc.js",
  "version": "0.3.3",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Broadshield/prettier-config.git",
    "directory": "."
  },
  "author": "Jamie Nelson <jamie@wearsafe.com>",
  "license": "Apache-2.0",
  "publishConfig": {
    "access": "public",
    "registry": "https://npm.pkg.github.com"
  },
  "keywords": ["prettier", "config", "prettier-config", "Broadshield"],
  "engines": {
    "node": ">=14.15.0"
  },
  "scripts": {
    "format": "yarn exec prettier --write . --config .prettierrc.js",
    "bump": "yarn version -i patch;git add .yarn package.json;git commit -m 'chore(release): bump version'",
    "bump:minor": "yarn version -i minor;git add .yarn package.json;git commit -m 'chore(release): bump version'",
    "build": "yarn pack --install-if-needed --out packages/%s-%v.tgz",
    "publish": "yarn npm publish --access public --tag v${npm_package_version}",
    "postversion": "git push origin --follow-tags",
    "precommit": "yarn exec pretty-quick --staged",
    "prepare": "yarn exec husky install"
  },
  "dependencies": {
    "prettier": "*"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "latest",
    "pretty-quick": "latest",
    "rimraf": "latest"
  },
  "optionalDependencies": {
    "@prettier/plugin-ruby": "*",
    "@prettier/plugin-xml": "*",
    "@typescript-eslint/parser": "*",
    "@typescript-eslint/typescript-estree": "*",
    "eslint": "*",
    "eslint-config-prettier": "*",
    "postcss": "*",
    "postcss-scss": "*",
    "prettier-package-json": "*",
    "prettier-plugin-java": "*",
    "prettier-plugin-properties": "*",
    "prettier-plugin-sh": "*",
    "stylelint": "*",
    "stylelint-config-prettier": "*",
    "typescript": "*",
    "typescript-eslint-language-service": "*",
    "yaml": "*"
  },
  "lint-staged": {
    "*": ["prettier --write", "git add"]
  },
  "packageManager": "yarn@3.2.1"
}
